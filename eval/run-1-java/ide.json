{"findings": [
  {"rule": "IDE-1", "file": "eval/fixture-java/src/main/java/com/roastable/billing/WebhookService.java", "line": 38, "evidence": "store.creditAccount(...) then events.save(...), no existsByEventId dedup before crediting", "fix": "Look up the event id (existsByEventId) and skip the credit if already processed, inside one transaction with the marker insert."},
  {"rule": "IDE-2", "file": "eval/fixture-java/src/main/java/com/roastable/billing/WebhookService.java", "line": 48, "evidence": "POST /v1/charges in a retry loop with no Idempotency-Key header", "fix": "Mint one idempotency key per charge, persist it, and send it as an Idempotency-Key header on every retry."},
  {"rule": "IDE-3", "file": "eval/fixture-java/src/main/java/com/roastable/billing/Store.java", "line": 34, "evidence": "account.getBalance().add(amount); account.setBalance(...); accounts.save(account)", "fix": "Credit with one atomic UPDATE (balance = balance + :amt) or add a @Version / PESSIMISTIC_WRITE lock."},
  {"rule": "IDE-3", "file": "eval/fixture-java/src/main/java/com/roastable/billing/Store.java", "line": 43, "evidence": "account.getBalance().subtract(amount); account.setBalance(...); accounts.save(account)", "fix": "Debit with one atomic UPDATE (SET balance = balance - :amt WHERE balance >= :amt) and treat 0 rows as insufficient funds."},
  {"rule": "IDE-4", "file": "eval/fixture-java/src/main/resources/db/schema.sql", "line": 40, "evidence": "event_id VARCHAR(255), nullable, no UNIQUE on the webhook_events dedup column", "fix": "Make it event_id VARCHAR(255) NOT NULL UNIQUE so the DB is the final dedup backstop."},
  {"rule": "IDE-4", "file": "eval/fixture-java/src/main/resources/db/schema.sql", "line": 34, "evidence": "external_ref VARCHAR(255), nullable, no UNIQUE on the payments external reference", "fix": "Add a UNIQUE constraint (NOT NULL) on payments.external_ref to reject duplicate provider references."},
  {"rule": "IDE-4", "file": "eval/fixture-java/src/main/java/com/roastable/billing/WebhookEvent.java", "line": 22, "evidence": "@Column(name = \"event_id\") on eventId with no unique = true", "fix": "Add unique = true to the event_id @Column so JPA DDL and the DB enforce dedup."}
]}
